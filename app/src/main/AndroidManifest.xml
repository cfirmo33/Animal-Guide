<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.marcelo.animalguide">

    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <activity
            android:name=".activitys.ForgotPasswordActivity"
            android:parentActivityName=".activitys.LoginActivity" />
        <activity android:name=".activitys.MainActivity" />
        <activity
            android:name=".registers.RegisterONGActivity"
            android:parentActivityName=".activitys.ChooseActivity" />
        <activity
            android:name=".registers.RegisterVeterinaryActivity"
            android:parentActivityName=".activitys.ChooseActivity" />
        <activity
            android:name=".registers.RegisterStudentActivity"
            android:parentActivityName=".activitys.ChooseActivity" />
        <activity
            android:name=".registers.RegisterOwnerActivity"
            android:parentActivityName=".activitys.ChooseActivity" />
        <activity android:name=".activitys.ChooseActivity" />
        <activity android:name=".activitys.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>